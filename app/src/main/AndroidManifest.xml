<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aran.githubapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_github_logo_kecil"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_github_logo_kecil"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubApp">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.FavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.DetailUserActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>